// --- FILE: nextflow.config ---

// 1. Opciones Globales del Motor
docker {
    enabled = true
    runOptions = '-u $(id -u):$(id -g)'
}

// 2. Importar la configuración modular
includeConfig 'conf/params.config'
includeConfig 'conf/modules.config'