// Placeholder vacío. Se incluye cuando --pipeline no coincide con ningún
// pipeline con config propia, para que el includeConfig ternario del
// nextflow.config raíz sea un no-op válido bajo el parser estricto (v2).
